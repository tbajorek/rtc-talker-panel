import React from 'react';
import InitializationContainer from '../../containers/Initialization';

const Initialization = ({}) => (
  <div className="normal-page">
    <h1>Inicjalizacja konta</h1>
    <InitializationContainer />
  </div>
);

Initialization.title = 'Inicjalizacja konta';

export default Initialization;
